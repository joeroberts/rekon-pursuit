# VD2-05 — Board Task 3 successor delivery releases

## 2026-07-31 review-repair successor

**APPROVED:** one bounded repair release to the same held serial Implementer.
Fresh revised Architecture, QA, Security/privacy, and TPM gates accept exactly
five repairs: truthful native success/precise no-op, subject-bound History
baseline/+1 deltas, four fixed host-only invalid providers through the real
delegate, final-source retained VD2-04 ordering, and successful normal/reduced
motion observation from exactly one Board animation decision/application.

Only `PipelineBoardView.swift`, the existing `REKON_UI_TEST_HOST` block in
`RekonVisualTheme.swift`, `RekonPursuitTests.swift`, and
`RekonPursuitUITests.swift` are writable. Project, `PipelineView`, scheme,
entitlements, Core/ViewModel dependencies, and non-target records are sealed.
The implementer must first reproduce the new full content-sensitive 75-record
manifest and all hashes/project/signing controls. Per direct owner sequencing,
after the repairs run only focused provider/native/invalid/History/motion smoke
and build the exact signed owner-facing Debug app. Delivery then requests a
non-final manual UX acceptance checkpoint. Do not run the combined pure 6/6,
Board UI 7/7, retained VD2-04 12/12, Core 114/114, ViewModel 91/91, or any
post-gate until explicit owner approval. After approval, that exact order and
all independent post-gates remain mandatory; no requirement is removed. Any
drift or later edit stops and restarts the applicable evidence. Task 3
acceptance, final owner handoff, status advancement, final VD2-05 acceptance,
and VD2-06–08 remain held.

## Historical project-registration successor

**Date:** 2026-07-31  
**Role:** Fresh independent Delivery Manager  
**Decision:** **APPROVED — supersede only the original five-file boundary with
the exact six-file Board Task 3 boundary below.**

## Release effect

The original Board release and its 73-record manifest remain immutable
historical evidence. The first implementer correctly stopped before RED after
discovering that the explicit-source Xcode project did not compile the required
new `PipelineBoardView.swift` in either app target. No Board source, test, or
project implementation edit occurred.

This successor changes one boundary fact only: it adds
`RekonPursuit.xcodeproj/project.pbxproj` as the sixth allowed path, solely for
minimal registration of the required Board source in the two existing app
targets. Every product behavior, test-first contract, evidence requirement,
review gate, serial-ownership rule, and downstream hold in the original
release remains controlling.

The same held fresh Implementer may resume because it stopped before RED and
edited none of the six implementation targets. Before editing, it must
reproduce this successor release in full. Project registration is Step 0; only
after valid registration, parse/list checks, and signed dual-target build proof
may it begin the first valid Board RED.

## Accepted amendment gates

| Gate | Evidence | Decision |
| --- | --- | --- |
| Planning | `.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-project-registration-amendment-planning-report-2026-07-31.md` | ACCEPT the conditional sixth path and exact dual-target registration contract. |
| Architecture | `.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-project-registration-amendment-architecture-gate-2026-07-31.md` | ACCEPT; minimal compile membership only, no ADR change. |
| QA | `.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-project-registration-amendment-qa-gate-2026-07-31.md` | ACCEPT; exact graph/count contract, signed baseline builds, and retained RED/GREEN matrix verified. |
| Security/privacy | `.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-project-registration-amendment-security-gate-2026-07-31.md` | ACCEPT; no signing, entitlement, runtime, persistence, network, fixture, or privacy expansion. |
| TPM | `.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-project-registration-amendment-tpm-gate-2026-07-31.md` | ACCEPT; successor six-file release is the sole dependency-safe next action. |

The amended controlling Planning artifacts are bound to:

| Artifact | SHA-256 |
| --- | --- |
| `docs/superpowers/plans/2026-07-30-vd205-persisted-pipeline-stage-movement.md` | `0a040b2f9af12869390e2ad6399d9acdfff6ef5513d1401ba34c566cd335c9e3` |
| `docs/delivery/task-briefs/VD2-05-persisted-pipeline-stage-movement.md` | `240fbed99a966dd5b3cbf57bbfc936495a350d69e5e4b3ac91881e0501198b98` |

## Historical release preservation

These pre-successor records must remain byte-for-byte unchanged:

| Historical record | SHA-256 |
| --- | --- |
| Original durable five-file release, `VD2-05-board-task3-delivery-release-2026-07-31.md` | `cc7e5d4a5149f003edd2a754be3c711d22fcd4405149b1158db988785ad69e72` |
| Original SDD five-file release, `task-3-board-delivery-release.md` | `7f16f905d52ccc0b60c0167a993eb2a0ac8fbf563e6bab254cb27d16a3eb397c` |
| Original 73-record manifest file, `task-3-board-dirty-worktree-content-manifest.md` | `38f6828f3160fff1251d0cd3041011416d8c136f8305f01194763b107ee75466` |

The historical manifest retains raw porcelain SHA-256
`26e2ed5f616ffede627a4ab51db3615dba4b9eebbc442690078a4b0ff9eca549`
and canonical content-record SHA-256
`df4ad62fdcc2335bdfb361e444dc4d6c48d0f271145a810891a26f8910f6697c`.
Direct comparison found 71 current paths unchanged and exactly the two
Planning-owned plan/brief amendments above; there were no missing or
unexpected paths.

## Successor content-sensitive worktree boundary

The new complete post-governance/post-render manifest is:

`.superpowers/sdd/2026-07-30-vd205-persisted-pipeline-stage-movement/task-3-board-six-file-dirty-worktree-content-manifest-2026-07-31.md`

Its scope is every individual tracked or untracked path returned by
`git status --porcelain=v1 -z --untracked-files=all` after this release,
both SDD ledgers, dashboard JSON, roadmap, and both rendered dashboard pages
are final. It records status, byte count, and content SHA-256 for each path and
expands untracked directories to files. Ignored SDD files are outside that Git
status set and are excluded to prevent recursive self-manifesting.

The successor manifest is the only current implementation attribution
baseline. Inclusion in either manifest does not accept unrelated dirty
content.

## Immutable six-target baseline

All five existing release targets match their immutable Git blobs; the sixth
target remains absent:

| Target | Bytes | Release SHA-256 | Immutable Git blob |
| --- | ---: | --- | --- |
| `RekonPursuit.xcodeproj/project.pbxproj` | 56971 | `810aee620de8f74e8816f20a479117cdec2b196bd0baa4eee2f846eaabb51fc3` | `23754e7bb35284af824e2e9634cefa08d90da1a8` |
| `RekonPursuit/PipelineView.swift` | 32999 | `38e7aea825f32e1916a3821f7ebbd4f4914b3b5e5826af397760ccd8a82b16fb` | `bbb82a06b01be0defd78ff960593d4bc283ff883` |
| `RekonPursuit/RekonVisualTheme.swift` | 75498 | `48a779b307a2f4f1bfa26214dc90b53ee5dac084abdc599be3f03b4acb005e4c` | `3478c18e7e25ee775ec0bf3022d2589865cd8847` |
| `RekonPursuitTests/RekonPursuitTests.swift` | 23576 | `41946f113ce091bc7cba0053f63cba94f749981c3996f55d357bc8eead48424b` | `284e9c2d54f822711d5872327b1c76eadafdc5e3` |
| `RekonPursuitUITests/RekonPursuitUITests.swift` | 72885 | `0f0a8dc205c64915e0210321162e31efbcf7c3fc41e3034e17337bf47ce7774f` | `c4e18ef61f287ddf232c9dc383aa89048d72d7fe` |
| `RekonPursuit/PipelineBoardView.swift` | — | **ABSENT** | New-file baseline. |

Each existing blob was read back, matched the recorded byte count/SHA-256,
and reproduced its Git object ID. Any pre-edit mismatch, including loss of
new-file absence, returns the slice to Delivery.

## Project baseline and only permitted registration

The Planning-captured project baseline independently reproduces:

- `plutil -lint RekonPursuit.xcodeproj/project.pbxproj`: OK;
- `xcodebuild -list -project RekonPursuit.xcodeproj`: both
  `RekonPursuit` and `RekonPursuitUITestHost` targets and schemes present;
- 50 `PBXFileReference`, 70 `PBXBuildFile`, 5 `PBXSourcesBuildPhase`,
  5 `PBXNativeTarget`, 10 `PBXGroup`, and 3 `PBXTargetDependency` objects;
- 26 main-app and 26 UI-host source memberships;
- zero `PipelineBoardView.swift` project tokens.

The only permitted project delta is:

1. one `PBXFileReference` for `PipelineBoardView.swift`, inserted once in the
   existing `RekonPursuit` group;
2. exactly two `PBXBuildFile` objects, both pointing to that one reference;
3. one new membership in main-app sources phase
   `900000000000000000000001`; and
4. one new membership in UI-host sources phase
   `900000000000000000000007`.

The only existing-object edits are those three membership-array insertions.
Expected post-registration counts are 51 file references, 72 build files, 27
main-app source memberships, and 27 UI-host source memberships. Every group,
target, phase, dependency, product, configuration, setting, signing value,
entitlement, scheme, framework, resource, package, and all unrelated objects
must remain unchanged.

Before valid RED, preserve a zero-context diff against immutable project blob
`23754e7bb35284af824e2e9634cefa08d90da1a8`; record the after-edit bytes,
SHA-256, and immutable blob; reproduce the structural counts and memberships;
run `plutil -lint` and `xcodebuild -list`; then complete signed Debug builds of
both app targets and prove `PipelineBoardView.swift` compiled exactly once in
each. A compile, parse, list, membership, signing, or structural failure is not
valid RED.

## Signed release-baseline build proof

Delivery freshly rebuilt the unchanged pre-registration baseline from isolated
DerivedData roots:

| Target | DerivedData | Result | Signed executable SHA-256 |
| --- | --- | --- | --- |
| `RekonPursuit` | `/private/tmp/rekon-vd205-task3-sixfile-delivery-main-20260731-9peis3` | signed Debug build passed | `726630f39ceb840c855c3b157479e2c28cbbe811492a3cbee7191708f7a88e78` |
| `RekonPursuitUITestHost` | `/private/tmp/rekon-vd205-task3-sixfile-delivery-uihost-20260731-UgXvfG` | signed Debug build passed | `98a9e7704a7e831fd5d506c6d6f9ce074ac6bc9d564e70a56bc093bedc9631e5` |

Both products pass strict deep signature verification, report their expected
bundle identifiers and Apple Development Team `2UA854NLX4`, compile
`PipelineView.swift` once, and compile no absent Board source. These builds
validate the release baseline only; the implementer must produce new signed
post-registration builds containing `PipelineBoardView.swift` once per target.

## Accepted parent Task 2 dependency/result binding

| Accepted dependency | SHA-256 |
| --- | --- |
| `RekonPursuitCore/Workspace/WorkspaceModels.swift` | `18675156d9e94477466da56d239a02e97fa45464fa5270d2612cd70e928a10e6` |
| `RekonPursuitCore/Workspace/WorkspaceStore.swift` | `676e669f614dc180cea8bd33742bb07d058d61945f653e43efacbbae9a0ee138` |
| `RekonPursuit/WorkspaceViewModel.swift` | `8e392c309fa2bab2218682ef297d071d66781e69453924846e94754c7ddef93a` |
| `RekonPursuitCoreTests/WorkspaceStoreTests.swift` | `83d61ea4769da4c44c8466b5af76415f7e9ecebf974295ba036a424921dc2162` |
| `RekonPursuitTests/WorkspaceViewModelTests.swift` | `dced4c9f2fef6b0acc671d03b7a148d831bd3b038e257d20413fe32d5053fcdd` |

| Suite | Passed | App host executable | XCTest executable | Result `Info.plist` |
| --- | ---: | --- | --- | --- |
| Core | 114/114 | `adc5db8380d3392f42fb833b1b900dcabd0f888acd98c0be325abe79f28c979e` | `516c7cccaae05018578e6331a36acab5af2b8e879ef0ab7de31c9a243f5ceede` | `d0cc1d0d9d712e7cd060787363ef78dac6bb79a9c3fe68a2fc2de40bd7f61c6b` |
| ViewModel | 91/91 | `2e914541445cbb62d3384c86ee313ff4d08084bba114ebd1f06e95d6e75387fb` | `a88e123b93348d83cb6b58acebf7e8523233be4e9bd2d839f9c490e1b47b1e86` | `1623dd7583e2eed981bdce819eeff2665d0f450cda806312fefee33665396d3a` |

All five source hashes and all six retained artifacts reproduce at release.
They are read-only Task 3 dependencies. Any drift before or during Board work
is a stop condition.

## Exact six-file implementation boundary

The same held fresh Implementer owns all six paths serially. No parallel worker
may edit any target:

- create `RekonPursuit/PipelineBoardView.swift`;
- narrowly delegate Board rendering from `RekonPursuit/PipelineView.swift`;
- add only sealed `#if REKON_UI_TEST_HOST` scenario routing in
  `RekonPursuit/RekonVisualTheme.swift`;
- add pure/presentation Board contracts in
  `RekonPursuitTests/RekonPursuitTests.swift`;
- add signed Board workflow/accessibility contracts in
  `RekonPursuitUITests/RekonPursuitUITests.swift`;
- change `RekonPursuit.xcodeproj/project.pbxproj` only for the exact
  dual-target source registration above.

The implementer must reproduce the complete successor manifest, all target
hashes/blobs and new-file absence, both Planning hashes, all dependency/result
hashes, project graph, plist parse, and target list before any edit. It then
performs Step 0 registration and build proof before Board RED. It preserves
every non-target successor-manifest record byte-for-byte.

No Core, schema, migration, recovery, route, Table/inspector redesign,
dashboard, roadmap, ledger, delivery evidence, status, signing, entitlement,
scheme, target, dependency, product, framework, resource, package, unrelated
test, or other dirty path is authorized.

## Test-first and post-implementation proof

After Step 0, continue the original Board release unchanged:

- valid RED for pure ID-only payload and all invalid/cancelled/outside inputs;
- persisted-only relocation and exact lane/stage presentation;
- keyboard, VoiceOver, all six target labels, focus, live redacted outcome,
  and Reduce Motion parity;
- sealed success, no-op, blocked Close, unavailable, write failure, projection
  failure, closed-filter, relaunch, and History scenarios;
- exactly one new matching activity and one new history record for success;
- signed wide/compact native-drag, keyboard, accessibility, relaunch, History,
  filter, failure, motion, and retained VD2-04 regression evidence.

Before Board acceptance, prove the exact six-file baseline-relative diff and
successor-manifest preservation; retain project before/after bytes, hashes,
blobs, structural proof, signed builds, compile lists, result inventories, and
signatures; rerun complete signed Core 114 and ViewModel 91 selectors; and run
`git diff --check`.

Fresh separate Code Review and independent QA must accept first, followed by
Architecture, TPM, Security/privacy, and Delivery. The Implementer may not
review or verify its own work.

## Stop conditions and holds

Stop immediately for any manifest, target, Planning, dependency, result, or
project baseline mismatch; extra project hunk/object/membership; parse/list/
build/signing/fixture/cache failure; non-target drift; invalid setup RED; or
attempt to place the focused Board implementation inside `PipelineView.swift`.

This release does not accept Board Task 3, move a card, or release product
behavior. Owner smoke/handoff, `needsUserAction`, dashboard/roadmap/status
advancement, final VD2-05 acceptance, and VD2-06–08 remain blocked until every
post-implementation gate passes.
