# RP-R0 baseline summary

**Task:** `RP-R0`  
**Status:** Accepted under the amended evidence-only boundary.

## Verified findings

- The default first-launch page did not expose the workspace creation action.
- Workspace creation was available only after navigating to Add Opportunity.
- The default page did not explain why CSV import was unavailable before a workspace existed.
- The original root layout diverged from the approved desktop shell and clipped at a compact desktop window size.

## Evidence boundary

The original capture attempt encountered an isolation failure and was stopped.
All sensitive runtime details and screenshots from that attempt were removed from
repository history. The corrected R1a task owns the safe temporary-app runtime
verification of workspace creation and CSV preview.
